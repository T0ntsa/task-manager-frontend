# Most important prompts

## 1.

Do me a react frontend for showing all tasks. Backend https://github.com/T0ntsa/BackendProject

Specifically front end to view all tasks api -> /api/tasks
Make sure there is auth check is user "admin" or not.

## 2. 

explain this "Then replace the default files with the components above.". 
So when i use command "npx create-react-app task-manager-frontend" there will be react app 
in folder named task-manager-frontend. Then move to that folder and istall package axion. 
What does axion do? In task-manager-frontend folder, where exactly i put files you provided?
    2. Prompt answers
    Axion -> fetch() but better features
        Key benefits of Axios:
        Automatically converts JSON responses
        Better error handling
        Works in older browsers
        Can intercept requests/responses
        Easier to set global headers
    -> File tree

## 3.
Next feature is to make it look much nicer. 
Now tasks are in a table where each row is one element. 
I want each element to be its own card. 
Status and priority much smaller but keep the coloring, helps to identify urgency.
Card title should be element.title on left, right side priority. 
Next line description and other info. Bottom left status and bottom right duedate.

